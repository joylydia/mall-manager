<?xml version="1.0" encoding="UTF-8"?>
<!--
严肃声明：
开源版本请务必保留此注释头信息，若删除我方将保留所有法律责任追究！
本软件已申请软件著作权，受国家版权局知识产权以及国家计算机软件著作权保护！
可正常分享和学习源码，不得用于违法犯罪活动，违者必究！
Copyright (c) 2020 十三 all rights reserved.
版权所有，侵权必究！
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ltd.newbee.mall.dao.MallShoppingCartItemMapper">
    <resultMap id="BaseResultMap" type="jl.mall.entity.MallShoppingCartItem">
        <id column="cart_item_id" jdbcType="BIGINT" property="cartItemId"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="goods_id" jdbcType="BIGINT" property="goodsId"/>
        <result column="goods_count" jdbcType="INTEGER" property="goodsCount"/>
        <result column="status" jdbcType="TINYINT" property="status"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>
    <sql id="Base_Column_List">
    cart_item_id, user_id, goods_id, goods_count, status, create_time, update_time
  </sql>

    <select id="selectByUserIdAndGoodsId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from jl_mall_shopping_cart_item
        where user_id = #{newBeeMallUserId,jdbcType=BIGINT} and goods_id=#{goodsId,jdbcType=BIGINT} and status = 0
        limit 1
    </select>
    <select id="selectByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from jl_mall_shopping_cart_item
        where user_id = #{newBeeMallUserId,jdbcType=BIGINT} and status = 0
        limit #{number}
    </select>
    <select id="findMyNewBeeMallCartItems" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from jl_mall_shopping_cart_item
        where user_id = #{userId,jdbcType=BIGINT} and status = 0
        <if test="start!=null and limit!=null">
            limit #{start},#{limit}
        </if>
    </select>
    <select id="getTotalMyNewBeeMallCartItems" resultType="int">
        select
        count(*)
        from jl_mall_shopping_cart_item
        where user_id = #{userId,jdbcType=BIGINT} and status = 0
    </select>
    <select id="selectByUserIdAndCartItemIds" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from jl_mall_shopping_cart_item
        where
        cart_item_id in
        <foreach item="id" collection="cartItemIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        and user_id = #{newBeeMallUserId,jdbcType=BIGINT} and status = 0
    </select>
    <select id="selectCountByUserId" resultType="int">
    select
    count(*)
    from jl_mall_shopping_cart_item
    where user_id = #{newBeeMallUserId,jdbcType=BIGINT} and status = 0
  </select>

    <update id="deleteBatch">
        update jl_mall_shopping_cart_item
        set status=1 where cart_item_id in
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>